import styled from "styled-components";

export const DefaultMessage = styled.div`
  width: 100%;
  display: flex;
  padding: 2rem 0;
  font-size: 1.2rem;
  align-items: center;
  justify-content: center;
  color: var(--white);
`;
